@page
@model NpsPesquisa.Front.Pages.Perfis.IndexModel
@{
    ViewData["Title"] = "Perfis";
}

<h2>Perfis</h2>
<a asp-page="Create" class="btn btn-success mb-3">Novo Perfil</a>
<table class="table table-bordered">
    <thead>
        <tr>
            <th>Nome</th>
            <th>Descrição</th>
            <th>Ações</th>
        </tr>
    </thead>
    <tbody>
    @foreach (var p in Model.Perfis)
    {
        <tr>
            <td>@p.Nome</td>
            <td>@p.Descricao</td>
            <td>
                <a asp-page="Edit" asp-route-id="@p.Id" class="btn btn-primary btn-sm">Editar</a>
                <a asp-page="Details" asp-route-id="@p.Id" class="btn btn-info btn-sm">Detalhes</a>
                <form method="post" asp-page-handler="Delete" asp-route-id="@p.Id" style="display:inline;">
                    <button type="submit" class="btn btn-danger btn-sm">Excluir</button>
                </form>
            </td>
        </tr>
    }
    </tbody>
</table> 